package com.gmail.waylacteal;

import org.junit.AfterClass;
import org.junit.Assert;
import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

public class classic {

   /// WebElement loginField = driver.findElement(By.id("login"));
    ///    loginField.sendKeys("autotestorgua");
    ///WebElement passwordField = driver.findElement(By.id("password"));
    ///    passwordField.sendKeys("testpass");
    ///WebElement loginButton = driver.findElement(By.xpath("//button[text()='Войти']"));
     ///   loginButton.click();
    ///WebElement profileUser = driver.findElement(By.cssSelector(".login-button__user"));
    ///String mailUser = profileUser.getText();
     ///   Assert.assertEquals("autotestorgua@ukr.net", mailUser);///


    ////@AfterClass
    ///public static void tearDown() {
    ///    WebElement menuUser = driver.findElement(By.cssSelector(".login-button__menu-icon"));
    ///    menuUser.click();
    ///    WebElement logoutButton = driver.findElement(By.id("login__logout"));
    ///    logoutButton.click();
///
}

/*
    @Test
    public void lal() {
        int count = 6;
        String ALPHA_NUMERIC_STRING = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
        StringBuilder builder = new StringBuilder();
        while (count-- != 0) {
            int character = (int)(Math.random()*ALPHA_NUMERIC_STRING.length());
            builder.append(ALPHA_NUMERIC_STRING.charAt(character));
        }
        String b = builder.toString();
        System.out.println(b);
    }*/

    /*@Test
    public void lal() {
        int count = 6;
        String ALPHA_NUMERIC_STRING = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
        StringBuilder builder = new StringBuilder();
        while (count-- != 0) {
            int character = (int)(Math.random()*ALPHA_NUMERIC_STRING.length());
            builder.append(ALPHA_NUMERIC_STRING.charAt(character));
        }
        String b = builder.toString();
        System.out.println(b);*/

//WebElement YESNO = driver.findElement(By.className("RveJvd.snByac"));
//WebElement YESNO =driver.findElement(By.xpath("//div[@class='ZFr60d CeoRYc']/content[@class='CwaK9']/span[@class='RveJvd snByac']"));
//YESNO.click();
//WebElement button = driver.findElement(By.cssSelector("form[name=\"survey-form\"] div:nth-child(8) > label:nth-child(3) > button[type=\"button"));
